extends ../layout

block content
  h2  #{record.title+" "}
      i   (#{record.group})
  div.row
    div.col-md-7 
      img.rfloat.img-rounded(src="/images/covers/"+record.rid+"/"+record.cover, width="180px")
      |#{record.review}
    div.col-md-4 Состав:
      each m in record.members
        li
          a(href='/records/')=m.name
  hr
 
